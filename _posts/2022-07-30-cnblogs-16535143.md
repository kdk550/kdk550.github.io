---
layout: post
title: "P5318 【深基18.例3】查找文献"
date: 2022-07-30 15:47:00 +0800
updated: 2022-07-31 10:09:00 +0800
description: "原题地址：P5318 【深基18.例3】查找文献 根据描述和样例 分析如下： 1. 先对 边 u- v 排一个序，满足字典序的需求 2. dfs和bfs 都要记录当前点是否已经被访问过了，若之前没被访问过，继续dfs或bfs。防止出现重复访问 代码如下:"
excerpt: "原题地址：P5318 【深基18.例3】查找文献 根据描述和样例 分析如下： 1. 先对 边 u- v 排一个序，满足字典序的需求 2. dfs和bfs 都要记录当前点是否已经被访问过了，若之前没被访问过，继续dfs或bfs。防止出现重复访问 代码如下:"
categories: []
tags: ["graph theory"]
source_categories: ["图论"]
source_platform_tags: []
promoted_body_tags: []
canonical: "https://www.cnblogs.com/magicat/p/16535143.html"
source_url: "https://www.cnblogs.com/magicat/p/16535143.html"
permalink: /blog/2022/cnblogs-16535143/
cnblogs_post_id: 16535143
comments: false
related_posts: false
---
{% raw %}
原题地址：[P5318 【深基18.例3】查找文献](https://www.luogu.com.cn/problem/P5318)

根据描述和样例 分析如下：

1. 先对 边 **u->v 排一个序，满足字典序的需求**
2. **dfs和bfs 都要记录当前点是否已经被访问过了，若之前没被访问过，继续dfs或bfs。防止出现重复访问**

**代码如下:**



```
#include<iostream>
#include<cstring>
#include<vector>
#include<set>
#include<map>
#include<queue>
#include<algorithm>
using namespace std;


int n,m;
int d1[100010],d2[100010];
bool st[100010];
vector<int> son[100010];
void dfs(int u)
{
    cout<<u<<" ";
    st[u]=true;
    for(auto v:son[u])
        if(st[v]==false)
            dfs(v);

}
void bfs()
{
    st[1]=true;
    queue<int> q;   q.push(1);
    while(!q.empty())
    {
        int u=q.front();q.pop();
        cout<<u<<" ";
        for(auto v:son[u])   
            if(st[v]==false)
            {
                st[v]=true;
                q.push(v);
            }
    }
}
int main()
{
    cin>>n>>m;
    for(int i=1;i<=m;i++)
    {
        int x,y;    cin>>x>>y;
        son[x].push_back(y);
    }
    for(int i=1;i<=n;i++)
        sort(son[i].begin(),son[i].end());
    dfs(1);    cout<<endl;
    memset(st,false,sizeof st);
    bfs();     cout<<endl;
    return 0;
}
```
{% endraw %}
